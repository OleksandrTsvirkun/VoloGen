<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <PackageTags>source-generator;roslyn;valuegen</PackageTags>
  </PropertyGroup>

</Project>
